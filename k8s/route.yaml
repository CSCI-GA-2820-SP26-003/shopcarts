apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: shopcarts
  labels:
    app: shopcarts
spec:
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  to:
    kind: Service
    name: shopcarts
    weight: 100